## Packages
date-fns | Date formatting for agenda and shifts
recharts | Charts for financial stats
framer-motion | Smooth page transitions and UI animations
clsx | Class name utility (often used with tailwind-merge)
tailwind-merge | Class merging for components

## Notes
- Auth is handled via Replit Auth (use-auth.ts)
- AI Chat uses SSE streaming at /api/conversations/:id/messages
- Medical calculator should be accessible globally via floating button
- Shift stats endpoints require charts
